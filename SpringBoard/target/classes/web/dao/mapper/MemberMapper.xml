<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="web.dao.face.MemberDao">

	<select id="selectCnt" resultType="int" parameterType="web.dto.Member">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE id = #{id } AND pw = #{pw }
	</select>
	
	<select id="selectCntById" resultType="int">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE id = #{id }
	</select>
	
	<select id="selectNickByMember" resultType="String" parameterType="web.dto.Member">
		SELECT nick FROM MEMBER
		WHERE id = #{id }
	</select>
	
	<insert id="insertMember" parameterType="web.dto.Member">
		INSERT INTO MEMBER (id, pw, nick)
		VALUES (#{id }, #{pw }, #{nick })
	</insert>

</mapper>